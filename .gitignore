# Telegram-notifier и облачный воркер — приватные инструменты, не публикуем.
# В репозитории только watch.py (личный локальный сигнал для самостоятельного
# бронирования).
notifier.py
notifier_config.json
notifier_config.example.json
notifier_state.json
start_notifier.bat
start_notifier_sound.bat
install_autostart.ps1
uninstall_autostart.ps1
README_NOTIFIER.txt
cf-worker/

# Логи и runtime-состояние
*.log
baseline.txt

# Python
__pycache__/
*.pyc
