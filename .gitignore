# Личные секреты и настройки — НЕ публикуем (токен бота, chat_id, dedup-токен)
notifier_config.json

# Личные операционные заметки про мой конкретный деплой (реальный chat_id,
# ссылки на приватный воркер и т.п.) — не для публикации, README.md заменяет
# её как публичная документация.
README_NOTIFIER.txt

# Логи и runtime-состояние
*.log
notifier_state.json
baseline.txt

# Python
__pycache__/
*.pyc

# Облачная часть (воркер) — приватная, не публикуем; в репозитории только
# локальные инструменты (notifier.py / watch.py).
cf-worker/
